#nullable enable

namespace BinanceP2pMonitor.Constants;

/// <summary>
/// Enumeration for price movement direction when an alert triggers
/// </summary>
public enum AlertDirection
{
    Unknown = 0,
    Up = 1,
    Down = 2
}
