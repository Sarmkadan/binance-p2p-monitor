# FormatHelper

Provides centralized string formatting utilities for consistent presentation of financial, temporal, and numerical data across the `binance-p2p-monitor` application. All methods are stateless and operate on input parameters without modifying external state.

## API

### `FormatCurrency`
