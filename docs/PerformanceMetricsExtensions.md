# PerformanceMetricsExtensions

Provides extension methods for analyzing and summarizing performance metrics collected during operations within the `binance-p2p-monitor` project. These methods allow querying aggregated statistics such as success rates, durations, failure counts, and temporal execution data from a collection of `PerformanceMetrics` instances.

## API

### `GetSuccessRate`
