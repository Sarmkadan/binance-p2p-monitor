# CommandFactoryExtensions

`CommandFactoryExtensions` provides utility methods for dynamically creating and querying command instances within the `binance-p2p-monitor` project. These extensions facilitate the discovery, validation, and instantiation of commands registered in the application's command system, enabling runtime flexibility and command-driven workflows.

## API

### `CreateCommand`
